#include "main.h"

/**
 * print_alphabet - Entry point
 *
 * Description: prints alphabet and new line
 * Return: always returns void
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	return (0);
}
